import React from 'react'
import { useState } from 'react';

export default function FindByCategory() {


    // const [products, setProducts] = useState([]);

    // const getData = async ()=>{
    //   const data = await axios.get(`${import.meta.env.VITE_LOCAL_HOST}/${import.meta.env.VITE_PRODUCTS_LIST}/category/${categoryId}`) // Assuming categoryId is available in the scope
    //   console.log(data.data)
    //   setProducts( data.data)
    // };

  return (
    <>

        

    </>
  )
}
